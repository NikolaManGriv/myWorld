1. In streamlit you cannot change a widget value from source code
2. Using a "Send" button is quite easy and a fancy way to solve 1. problem, because the UI does not refresh once text boxes get values but when you press the button
3. Streamlit accepts markdown 